import 'package:flutter/material.dart';

/// Bundled emoji font family name declared in pubspec.
const kBundledEmojiFontFamily = 'NotoColorEmoji';

/// Bundled app default UI font family.
const kAppFontFamily = 'OPPOSans40';

/// Emoji-only style. Should only be applied to known emoji clusters.
const kEmojiTextStyle = TextStyle(fontFamily: kBundledEmojiFontFamily);
